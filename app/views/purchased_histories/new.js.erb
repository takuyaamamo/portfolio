// index.html.erb中のDivに対して、新規作成用のformをレンダリング
console.log('hi')
$("#CartItem").html("<%= escape_javascript(render 'cart', cart: @cart, purchased_history: @purchased_history, purchased_item: @purchased_item) %>")
// デフォルトのbootstrapでmodalを開いても良いが、タイミングが早すぎてrender間に合わない
$("#CartModalCenter").modal("show")
