// index.html.erb中のDivに対して、新規作成用のformをレンダリング
console.log('hi');
$("#ShowItem").html("<%= escape_javascript(render 'show', item: @item, tax_included: @tax_included) %>");
// デフォルトのbootstrapでmodalを開いても良いが、タイミングが早すぎてrender間に合わない
$("#ItemModalCenter").modal("show");
